name: full_cheap_pilot_sascore
description: 10K-call pilot for synthetic-accessibility desirability comparison.
tier: full
oracle_set: explicit
oracles:
  - sascore
algorithm_set: explicit
algorithms:
  - screening
  - graph_ga
  - smiles_ga
  - stoned
  - gpbo
smi_file: benchmark_inputs/zinc_sanity_1k.smi
output_root: oracle_benchmark_results
n_jobs: 1
skip_existing: true
continue_on_error: true
notes:
  objective_direction: maximize
  oracle_transform: SA score is converted to 10 - SA, so higher means easier synthesis
  seeds: full tier defaults to 0, 1, 2, 3, 4
  budget: full tier defaults to 10000 oracle calls
  reason: add a 10K lead-relevant cheap oracle alongside QED and LogP for PI-meeting comparison
